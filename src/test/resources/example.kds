kd:meta {
  version 1.0.0-beta
}

# TODO - Natural package and import syntax

/**
 * An example of KD Schema (KDS)
 *
 * KDS is written in KD using the kd.kds schema. Because of bare Strings we can use
 * Syntax that is very similar to classes defined in languages like Kotlin, TypeScript
 * and Swing.
 */
tag customer {
    firstName String
    lastName String
    age Int
    member Bool = false
    affiliateReferrals List[String] # referral IDs
    phoneNumbers Map[String, String] # home, business, etc. = phone number

    children {
        purchase 0.._ # zero or more Purchase tags
    }
}

# Example
Customer firstName="Jean-Luc" lastName="Picard" age=68

tag Button {
    kd:value String # Text
    action String = ""
    enabled Bool = true
}

# Example
Button "Close" action="exit"